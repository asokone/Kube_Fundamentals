
apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config
data:
  config: |
    address-pools:
    - name: default
      protocol: layer2
      addresses: 
      - 172.30.2.100-172.30.2.200  # Ensure this matches your node's subnet, adjust based on your network . . . 
#Please use the range of the node01 172.30.2.100-172.30.2.200
# We are using on premises for the cluster the 172.30.2.0.X network
# Use [kubectl get nodes –o wide ] the query your cluster IPs
# I am using this set of IPs: 172.30.2.100-172.30.2.200
